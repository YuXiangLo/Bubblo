using UnityEngine;

public class PlayerState
{
    // Player State Enum
    public enum PlayerStateType
    {
        Idle = 0,
        Run = 1,
        Jump = 2,
        Fall = 3,
        GenerateBubble = 4,
        Attack = 5
    }
}

